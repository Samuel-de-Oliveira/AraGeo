/* Solids.js
 *
 *  This is the modules that keep all
 *  the solids fuction that will be shown
 *  to the "solid-info" tag
 *
 */

export function renderCube() {
  return `It works!`;
}

export function renderCilinder() {
  return `It works!`;
} 

export function renderSphere() {
  return `It works!`;
}
